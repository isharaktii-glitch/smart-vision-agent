export const metadata = {
  title: 'Smart Vision Agent',
  description: 'Face Detection & Mood Analysis System',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
